// Find the maximum and minimum element in an array

#include <iostream>
#include <vector>
using namespace std;

vector<int> MaxiAndMini(int arr[], int n)
{
    vector<int> v;
    int max;
    int min;

    
}

int main()
{
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
}